{
  "title": "Right Shift → Hyper Key (⌃⌥⌘) (Escape if alone)",
  "rules": [
    {
      "description": "Right Shift → Hyper Key (⌃⌥⌘) | Escape if alone",
      "manipulators": [
        {
          "type": "basic",
          "from": {
            "key_code": "right_shift"
          },
          "to": [
            {
              "key_code": "left_command",
              "modifiers": ["left_control", "left_option"]
            }
          ],
          "to_if_alone": [
            {
              "key_code": "escape"
            }
          ]
        }
      ]
    }
  ]
}
